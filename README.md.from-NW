# 豆包去水印 · Doubao No-Watermark

> 一套用于**下载豆包（doubao.com）/ 即梦 / 通义千问 对话页与原图、无水印视频**的工具集合。
> 包含：浏览器插件（主战场）、微信小程序、以及大量第三方参考项目的审计整合。
>
> 🔒 本仓库为**私有库**，仅作者可见。

---

## 📑 目录索引

| 目录 / 文件 | 是什么 | 何时看 |
|-------------|--------|--------|
| **发布包/** | 浏览器插件成品源码（当前主版本 **V21 `豆包视频去水印` v2.1.0**）+ 宣传截图 | 想用 / 改插件时 |
| └ `发布包/doubao-edg-V21/` | V21 插件源目录（manifest / background / content / inject / popup / icons） | 读插件代码 |
| └ `发布包/doubao-edg-V21-screenshots/` | 商店上架用的 1280×800 宣传图 | 上架素材 |
| **小程序/** | 微信小程序版「云龙清图助手」等源码 | 小程序方案 |
| **历史版本/** | 插件历代版本归档（V1~V20、doubao-v170 系列、image-only 系列、旧 zip 备份） | 查旧实现 / 回滚 |
| **参考项目/** | 14 个第三方开源项目 + Dola 扩展样本（已审计，详见下方） | 研究别人怎么做 |
| **文档/** | 分析报告、修复记录、方向梳理（md + xlsx） | 看决策过程 |
| **analysis/** | 反编译 / 接口验证报告 | 深入原理 |
| **参考项目代码审计与整合.md** | ⭐ **核心文档**：18 项目审计 + 六大机制分类 + 带标注整合代码 + 对 V21 启示 | 想一眼看懂全局 |

---

## 🚀 浏览器插件 快速开始（V21）

1. 打开 Edge / Chrome → 扩展管理（`edge://extensions` 或 `chrome://extensions`）
2. 开启右上角 **「开发者模式」**
3. 点 **「加载已解压的扩展程序」** → 选择 `发布包/doubao-edg-V21/`
4. 打开豆包对话页，生成图片/视频后会自动出现无水印下载入口；原「保存」按钮也会走无水印逻辑

**支持范围**
- ✅ 豆包对话页：原图（`image_ori_raw`）+ 真·无水印视频（`fallback_api` 解码）
- ✅ 豆包分享页（`/s/xxx`）：视频 `get_play_info` → `main_url`
- ✅ 即梦 / 通义千问：图片原图直链

---

## 🧩 核心原理（六大去水印手法）

所有方案本质上只用了 6 种手法（详见 `参考项目代码审计与整合.md`）：

| 机制 | 作用 | 我们 V21 采用 |
|------|------|---------------|
| **M1 原图直链** | 抓接口里的 `image_ori_raw.url`（服务端真原图，无水印） | ✅ 图片 |
| M2 双图合并 | 两张互补干净图拼合（降级有损） | — |
| **M3 视频 lr 改写** | `get_play_info` 取 `main_url` + 强制无码参数 | ✅ 分享页视频 |
| M4 URL 重写 | `declarativeNetRequest` 剥 `-watermark` 后缀 | — |
| M5 算法消除 | inpaint / 扩散重绘（像素级修复） | — |
| M6 15 秒解锁 | 改 `duration=15` 或覆盖 skill-pack 响应 | — |

> **关键共识**：图片去水印真答案 = `image_ori_raw.url`；视频 = `get_play_info` 的 `original_media_info.main_url` + 强制无码参数。其余手法都是取不到原图时的有损降级。

---

## 📦 参考项目说明

`参考项目/` 收录了 14 个第三方开源项目 + 2 个 Dola 扩展样本，**均为各自作者所有的开源代码**，本仓库仅作研究存档，使用请遵守其原始许可。

代表性项目：
- `doubao-downloader-plus` / `ai-watermark-remover` — 多平台去水印扩展
- `ihmily-doubao-nomark` / `doubao-nomark-online` — Python / Next.js 后端方案
- `豆包15s+无水印 7-4(1)` — Dola 15 秒扩展样本（含 `fallback_api` 机制来源）
- `ComfyUI-remove-ai-watermarks` / `declank` — 算法级（像素）去水印
- 多个 Greasemonkey 油猴脚本 — 轻量前端方案

---

## 📝 文档导航

- `参考项目代码审计与整合.md` — **入口文档**，先读这个
- `文档/豆包视频去水印_技术分析报告.md` — 技术原理深潜
- `文档/版本修复记录.xlsx` — 各版本迭代与 bug 修复流水

---

## ⚠️ 免责声明

本仓库代码仅用于**个人学习与研究**，帮助用户在**自己生成的内容**上去除平台叠加的水印。
请勿用于抓取、传播他人版权内容。使用即代表你理解并同意自行承担相关风险。
